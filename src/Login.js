import './Login.css'
import TitleBar from './TitleBar';

function Login() {
    return (
        <div>
            <TitleBar state={'login'}></TitleBar>
            LogIn page!
        </div>
    )
}

export default Login;